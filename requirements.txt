fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.8.2
starlette==1.3.1           # CVE-2026-54283, CVE-2026-54282 — explicit pin of fastapi transitive dep

# Auth & database
sqlalchemy==2.0.50
passlib[bcrypt]==1.7.4
bcrypt==4.0.1       # passlib 1.7.4 is not compatible with bcrypt 5.x
python-jose[cryptography]==3.5.0
cryptography==49.0.0           # GHSA-537c-gmf6-5ccf — explicit pin of python-jose/google-auth transitive dep

# Stage 1 – Speech-to-Text (Google Chirp)
google-cloud-speech==2.27.0

# Stage 2 – Acoustic features
librosa==0.10.2
soundfile==0.12.1
speechbrain==1.0.2

# Stage 3 – Morphological tagging
spacy==3.7.5
# After install: python -m spacy download en_core_web_sm

# Stage 4 – Semantic embeddings
sentence-transformers==3.1.1
scikit-learn==1.5.2
scipy>=1.11

# Stage 5 – Emotion detection
transformers==4.44.2
torch==2.12.1              # CVE-2025-3000 — updated from stale 2.4.1 pin (2.12.0 was already installed)
